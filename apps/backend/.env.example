PORT=3001
DATABASE_URL=          # Supabase service role connection string
DATABASE_SSL=false
CLINIC_STATUS_SCHEMA=clinic
SESSION_SECRET=        # Generate a strong random string
CMS_URL=               # Railway Strapi service URL
JWT_ACCESS_SECRET=     # Generate a strong random string
JWT_REFRESH_SECRET=    # Generate a strong random string
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
AUTH_SEED_EMAIL=       # Email for the initial admin user
AUTH_SEED_PASSWORD=    # Password for the initial admin user
AUTH_SEED_ROLES=admin
CORS_ORIGIN=http://localhost:3000
